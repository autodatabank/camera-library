pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url "https://jitpack.io"
        }
//        maven {
//            ${project.buildDir}/repo
//            url "/Users/oooobang/asp/camera/app/build/outputs/bundle/release/"
//            //url "/Users/oooobang/asp/camera/app/build/outputs/bundle/release/"
//        }
    }
}
rootProject.name = "camera"
include ':app'
include ':cameralibrary'
